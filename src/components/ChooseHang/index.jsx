import './styles'

export default function ChooseWorkout (workouts) {
    function pick (option) {
        return (
            
        )
    }
    return (
        <>
            <button onClick={()=>}></button>
        </>
    )
}